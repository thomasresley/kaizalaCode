{
  "name": "kaizala-test",
  "version": "1.0.0",
  "description": "kaizala training test",
  "main": "index.js",
  "author": "Thomas Rakwach <thomasrakwach@gmail.com>",
  "private": true,
  "scripts": {
    "run": "npm install && nodemon index.js --env development",
    "start": "npm install --save-dev sequelize-cli && npm install && cd datasource && npx sequelize-cli db:migrate --env production && cd ../ && node index.js"
  },
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "mysql2": "^2.1.0",
    "nodemon": "^2.0.4",
    "pg": "^8.3.3",
    "sequelize": "^5.21.7"
  },
  "engines": {
    "node": "10.x"
  },
  "devDependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "sequelize-cli": "^6.2.0"
  }
}
